[Unit]
Description=StateFS FUSE filesystem, system-wide

[Service]
ExecStartPre=/usr/bin/statefs-prerun
ExecStart=/usr/bin/statefs --system -oallow_other /run/state -f
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target

