[Unit]
Description=StateFS FUSE filesystem
After=syslog.target

[Service]
ExecStartPre=/bin/sh mkdir /var/run/state
ExecStart=/usr/bin/statefs /var/run/state
Restarts=Always
RestartSec=1

[Install]
WantedBy=basic.target
